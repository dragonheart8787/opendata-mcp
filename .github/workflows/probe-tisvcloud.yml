name: Probe tisvcloud.freeway.gov.tw (throwaway)

# ONE-OFF diagnostic — see scripts/probe-tisvcloud.ts's header comment.
# Delete this workflow once real endpoint answers are in hand.

on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - run: npx tsx scripts/probe-tisvcloud.ts
